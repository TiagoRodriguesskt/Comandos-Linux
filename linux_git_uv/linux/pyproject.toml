[project]
name = "linux"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "TiagoRodriguesskt", email = "tiagorodriguesskt@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "requests>=2.34.2",
]

[project.scripts]
linux = "linux:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
